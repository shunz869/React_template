import React, { Component } from 'react';

export default class FirProducts extends Component{
  render(){
    return (
      <div className="row">
        this is the first item list
      </div>
    );
  };
}