import React, { Component } from 'react';

export default class SecProducts extends Component{
  render(){
    return (
      <div className="row">
        this is the Second item list
      </div>
    );
  };
}